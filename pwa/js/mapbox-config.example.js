/**
 * Copie para mapbox-config.js e preencha o token público (pk.) do Mapbox.
 * Em produção (Vercel): defina MAPBOX_ACCESS_TOKEN nas variáveis de ambiente.
 */
export const MAPBOX_ACCESS_TOKEN = '';
